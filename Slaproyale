local Library = loadstring(Game:HttpGet("https://raw.githubusercontent.com/bloodball/-back-ups-for-libs/main/wizard"))()

local PhantomForcesWindow = Library:NewWindow("Kifoot hub")

local KillingCheats = PhantomForcesWindow:NewSection("option")

KillingCheats:CreateButton("Antilag", function()
-- Get the ReplicatedStorage service
local ReplicatedStorage = game:GetService("ReplicatedStorage")

-- Find the folder named "Assets" in ReplicatedStorage
local assetsFolder = ReplicatedStorage:FindFirstChild("Assets")

-- Check if the folder exists
if assetsFolder and assetsFolder:IsA("Folder") then
    -- Remove the folder
    assetsFolder:Destroy()
    print("Assets folder removed from ReplicatedStorage.")
else
    print("Assets folder not found in ReplicatedStorage.")
end
end)

KillingCheats:CreateButton("Server Killer", function()
local players = game:GetService("Players")
local replicatedStorage = game:GetService("ReplicatedStorage")
local localPlayer = players.LocalPlayer
local character = localPlayer.Character or localPlayer.CharacterAdded:Wait()
local missileLauncher = character:FindFirstChild("Missile-Launcher")

if missileLauncher then
    for _, player in pairs(players:GetPlayers()) do
        if player ~= localPlayer and player.Character then
            local args = {
                [1] = missileLauncher,
                [2] = player.Character
            }
            replicatedStorage.Events.Ability:FireServer(unpack(args))
        end
    end
else
    warn("Missile-Launcher not found in LocalPlayer's character.")
end
end)
